---
import BaseLayout from '../layouts/BaseLayout.astro';
import { sitePath } from '../utils/sitePath';
---
<BaseLayout
  title="页面没有找到｜Tomme"
  description="当前地址没有对应页面，可以返回首页或继续查看Tomme的项目。"
  robots="noindex, follow"
>
  <section class="not-found page-shell">
    <div>
      <p class="code">404</p>
      <h1>这个地址没有对应页面。</h1>
      <p>链接可能已经变化，或者地址输入不完整。你可以返回首页了解整体内容，也可以直接进入五个项目。</p>
      <div class="button-row">
        <a class="button primary" href={sitePath('/')}>返回首页</a>
        <a class="button" href={sitePath('/#projects')}>查看项目</a>
      </div>
    </div>
    <aside aria-label="网站主要内容">
      <strong>网站仍然可以继续阅读</strong>
      <a href={sitePath('/method/')}>分析方法</a>
      <a href={sitePath('/ai-collaboration/')}>AI协作</a>
      <a href={sitePath('/about/')}>关于我</a>
    </aside>
  </section>
</BaseLayout>

<style>
  .not-found{min-height:calc(100vh - 180px);padding-block:clamp(5rem,10vw,9rem);display:grid;grid-template-columns:minmax(0,1.3fr) minmax(280px,.7fr);gap:5rem;align-items:center}.not-found>div{min-width:0}.code{margin:0 0 1rem;color:var(--coral);font-size:1rem;font-weight:900}.not-found h1{max-width:12ch;margin:0 0 1.5rem;font-size:clamp(3.5rem,7vw,6rem);line-height:1.04;letter-spacing:-.04em}.not-found>div>p:not(.code){max-width:62ch;font-size:1.08rem}.not-found aside{padding:2rem 0;display:grid;gap:.9rem;border-block:1px solid var(--line)}.not-found aside strong{margin-bottom:.35rem;color:var(--blue);font-size:1.2rem}.not-found aside a{width:max-content;color:var(--ink);font-weight:800;text-decoration:none}.not-found aside a:hover{color:var(--blue)}
  @media(max-width:820px){.not-found{grid-template-columns:1fr;gap:3rem;min-height:auto}.not-found h1{font-size:clamp(3rem,14vw,4.8rem)}}
</style>
